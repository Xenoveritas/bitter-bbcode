{
  "name": "bitter-bbcode",
  "version": "0.0.1",
  "description": "A module for parsing BBCode and translating it to HTML.",
  "repository": "Xenoveritas/bitter-bbcode",
  "main": "./lib/bbcode.js",
  "bin": "./cli.js",
  "scripts": {
    "prepublish": "globstar -- coffee -o lib -c src/*.coffee",
    "doc": "codo src/",
    "pretest": "globstar -- coffee -o lib -c src/*.coffee",
    "test": "mocha --compilers coffee:coffee-script/register"
  },
  "keywords": [
    "bbcode"
  ],
  "author": "Xenoveritas",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "codo": "^2.1.0",
    "coffee-script": "^1.10.0",
    "globstar": "^1.0.0",
    "mocha": "^2.5.3"
  }
}
