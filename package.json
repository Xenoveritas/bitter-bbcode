{
  "name": "bbcode",
  "version": "1.0.0",
  "description": "A module for parsing BBCode and translating it to HTML.",
  "main": "./lib/bbcode.js",
  "bin": "./cli.js",
  "scripts": {
    "prepublish": "coffee -o lib -c src/bbcode.coffee",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bbcode"
  ],
  "author": "Xenoveritas",
  "license": "MIT",
  "devDependencies": {
    "coffee-script": "^1.10.0"
  }
}
